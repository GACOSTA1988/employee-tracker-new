<% if @project.errors.any? %>
  <h3>Please fix these errors:</h3>
  <ul>
    <% @project.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h1>Add a new project:</h1>

<%= form_for [@employee, @project] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <p><%= f.submit %></p>
<% end %>
